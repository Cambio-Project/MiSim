package de.rss.fachstudie.MiSim.parsing;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.io.File;

class ExpModelParserTest {

    @Test
    void ReadExperiment() {
//        File f = new File("./Examples/loon_mock_experiment_chaosmonkey.json");
//        ExpModelParser parser = new ExpModelParser(f.getAbsolutePath());
//        Assertions.assertFalse(ExpModelParser.simulation_meta_data.isEmpty());
//        Assertions.assertTrue(ExpModelParser.chaosmonkeys.length > 0);
//        Assertions.assertTrue(ExpModelParser.request_generators.length > 0);
//        Assertions.assertTrue(ExpModelParser.latencymonkeys.length > 0);
    }
}